# binaries (root only — cmd/janjacast/ is source!)
/janjacast
/janjacast.exe

# web
web/node_modules/
web/dist/*
!web/dist/index.html

# env
.env
.env.local

# Claude Code session artifacts (agent worktrees etc.)
.claude/
*~
*.exe
server.log
